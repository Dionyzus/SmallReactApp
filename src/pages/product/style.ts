import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    formControl: {
      minWidth: '100%',
      marginTop: 10,
    },
    root: {
      flexGrow: 1,
      maxWidth: 752,
    },
  }));